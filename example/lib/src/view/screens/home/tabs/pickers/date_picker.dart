import 'package:adp_desktop/adp_desktop.dart';

class DatePickerPreview extends StatelessWidget {
  const DatePickerPreview({super.key});

  @override
  Widget build(BuildContext context) {
    return const AdaptiveDatePicker();
  }
}
