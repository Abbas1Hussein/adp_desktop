import 'package:adp_desktop/adp_desktop.dart';

class TimePickerPreview extends StatelessWidget {
  const TimePickerPreview({super.key});

  @override
  Widget build(BuildContext context) {
    return const AdaptiveTimePicker();
  }
}
